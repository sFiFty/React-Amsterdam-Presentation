import React, { Component } from 'react';

class AsyncMode extends Component {
  render() {
    return (
      <div className="async-mode">
        AsyncMode
      </div>
    );
  }
}

export default AsyncMode;
